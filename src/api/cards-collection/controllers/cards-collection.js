'use strict';

/**
 * cards-collection controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cards-collection.cards-collection');
